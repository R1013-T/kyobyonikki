export type ConditionChartData = {
  date: string
  condition: number
}

export type User = {
  id: string
  name: string
  email: string
  password: string
}

export type OAuthUser = {
  id: string
  name: string
  email: string
  image: string
}